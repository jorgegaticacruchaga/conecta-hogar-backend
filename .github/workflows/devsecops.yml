name: Pipeline DevSecOps Backend

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  sast-semgrep:
    name: 1. SAST (Semgrep)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Escaneo de codigo Java
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/java

  sca-owasp:
    name: 2. SCA (OWASP Dependency-Check)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      - name: Escaneo de dependencias pom.xml
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: 'Backend-Spring-Boot'
          path: '.'
          format: 'HTML'

  container-trivy:
    name: 3. Container Security (Trivy)
    runs-on: ubuntu-latest
    needs: [sast-semgrep, sca-owasp]
    steps:
      - uses: actions/checkout@v4
      - name: Construir Imagen Docker
        run: docker build -t backend-spring:${{ github.sha }} .
      - name: Escanear Imagen con Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'backend-spring:${{ github.sha }}'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'